---
import { BrainCircuit, Code, FolderKanban, Briefcase } from "@lucide/astro";

const steps = [
  {
    id: 1,
    title: "Descubre tu potencial tech",
    description: "Evaluamos tus habilidades y motivación para asignarte al programa más adecuado. No importa tu nivel de experiencia - solo tu deseo de aprender.",
    icon: BrainCircuit
  },
  {
    id: 2,
    title: "Aprende haciendo, no solo estudiando",
    description: "Desde el primer día, trabajarás en proyectos reales. Nuestros sprints de aprendizaje intensivo combinan teoría esencial con práctica inmediata bajo mentoría experta.",
    icon: Code
  },
  {
    id: 3,
    title: "Crea un portafolio que impresione",
    description: "Desarrollarás proyectos completos que resuelven problemas reales del mercado peruano, construyendo un portafolio que demuestra tus capacidades a empleadores potenciales.",
    icon: FolderKanban
  },
  {
    id: 4,
    title: "Conecta con empresas tech líderes",
    description: "Te presentamos a nuestra red de empresas colaboradoras que buscan talento. Participarás en ferias de empleo exclusivas y entrevistas preparadas específicamente para graduates de Orbi.",
    icon: Briefcase
  }
];

const stats = [
  { value: "85%", label: "Empleabilidad en menos de 3 meses" },
  { value: "40%", label: "Más de salario inicial" },
  { value: "92%", label: "Empresas que contratan repetidamente" }
];
---

<section id="como-funciona" class="py-16 lg:py-24 bg-gradient-to-br from-white to-gray-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Encabezado de sección con estética MD3 -->
    <div class="text-center mb-12 lg:mb-20">
      <div class="inline-block mb-3">
        <div class="h-1.5 w-16 bg-red-800 rounded-full mb-2 mx-auto"></div>
      </div>
      <h2 class="text-3xl lg:text-4xl font-bold text-gray-900 mb-4 tracking-tight">
        Tu camino hacia el éxito <span class="text-red-800">tech</span> en Perú
      </h2>
      <p class="max-w-2xl mx-auto text-lg text-gray-600 leading-relaxed">
        Metodología intensiva que acelera tu inserción laboral en la industria tecnológica
      </p>
    </div>

   <!-- Proceso paso a paso -->
<div class="relative">
    <!-- Línea conectora vertical para desktop -->
    <div class="hidden lg:block absolute left-1/2 top-0 bottom-0 w-0.5 bg-gradient-to-b from-red-800/10 via-red-800/30 to-red-800/10 transform -translate-x-1/2"></div>
  
    <div class="space-y-16 lg:space-y-32 relative">
      {steps.map((step, index) => (
        <div class={`lg:grid lg:grid-cols-2 gap-8 lg:gap-16 items-center ${index % 2 === 0 ? "" : "lg:grid-flow-dense"} group`}> 
        
          <div class={`bg-white rounded-3xl p-8 shadow-sm group-hover:shadow-lg relative z-10 transform transition-all duration-500 group-hover:-translate-y-2 border border-gray-100/60 ${index % 2 === 0 ? "lg:text-right" : ""}`}>
            <!-- Elemento decorativo según MD3 -->
            <div class="absolute -right-4 -top-4 w-16 h-16 bg-gradient-to-br from-red-800/10 to-red-700/5 rounded-full opacity-0 group-hover:opacity-100 transition-opacity duration-700"></div>
            <div class="absolute -left-4 -bottom-4 w-20 h-20 bg-gradient-to-br from-red-800/10 to-transparent rounded-full opacity-0 group-hover:opacity-100 transition-opacity duration-700 delay-100"></div>
            
            <!-- Número y título -->
            <div class={`flex items-center mb-6 ${index % 2 === 0 ? "lg:justify-end" : ""}`}>
              <div class="h-14 w-14 rounded-2xl bg-gradient-to-br from-red-50 to-red-100 flex items-center justify-center mr-4 shadow-sm group-hover:shadow transition-shadow duration-300">
                <span class="text-red-800 font-bold text-xl">{step.id}</span>
              </div>
              <h3 class="text-2xl font-bold text-gray-900 group-hover:text-red-800 transition-colors duration-300">{step.title}</h3>
            </div>
            
            <!-- Contenido con mejor tipografía -->
            <p class="text-gray-600 leading-relaxed text-pretty">{step.description}</p>
            
            <!-- Indicador visual MD3 -->
            <div class={`mt-6 ${index % 2 === 0 ? "ml-auto" : ""} h-0.5 w-0 group-hover:w-16 bg-red-800 rounded-full transition-all duration-700 ease-out`}></div>
          </div>
  
          <!-- Ilustración -->
          <div class={`flex justify-center ${index % 2 === 0 ? "lg:order-first" : ""} hidden lg:flex`}>
            <div class="relative w-72 h-72 bg-gradient-to-br from-red-50 to-red-100 rounded-3xl flex items-center justify-center overflow-hidden shadow-md group-hover:shadow-lg transition-all duration-500">
              <!-- Elementos decorativos -->
              <div class="absolute inset-0 overflow-hidden opacity-20">
                <div class="absolute -right-10 -top-10 w-40 h-40 rounded-full bg-gradient-to-br from-red-800/20 to-red-700/5 animate-float-slow"></div>
                <div class="absolute -left-10 -bottom-10 w-32 h-32 rounded-full bg-gradient-to-tr from-red-800/20 to-transparent animate-float-medium"></div>
                
                <!-- Patrón sutil -->
                <div class="absolute inset-4 rounded-2xl border border-dashed border-red-800/10"></div>
              </div>
              
              <!-- Icono con animación -->
              <div class="relative z-10 transform transition-all duration-500 group-hover:scale-110 bg-white/80 backdrop-blur-sm p-5 rounded-2xl shadow-sm group-hover:shadow">
                <step.icon size={90} class="text-red-800" stroke-width={1.5} />
              </div>
            </div>
          </div>
  
          <!-- Marcador de paso para desktop -->
          <div class="hidden lg:block absolute left-1/2 transform -translate-x-1/2 w-12 h-12 bg-white rounded-full border-4 border-red-800/80 z-20 shadow-md" style={`top: ${50 + (index * 0) }%`}></div>
        </div>
      ))}
    </div>
  </div>

    <!-- Estadísticas -->
    <div class="mt-20 mb-16 py-12 px-6 bg-white rounded-3xl shadow-md relative overflow-hidden">
      <div class="absolute inset-0 overflow-hidden">
        <div class="absolute -right-10 -top-10 w-40 h-40 rounded-full bg-red-800/5"></div>
        <div class="absolute -left-10 -bottom-10 w-40 h-40 rounded-full bg-red-800/5"></div>
      </div>
      
      <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 relative z-10">
        {stats.map((stat) => (
          <div class="text-center">
            <div class="text-4xl font-bold text-red-800 mb-2">{stat.value}</div>
            <p class="text-gray-600">{stat.label}</p>
          </div>
        ))}
      </div>
    </div>

    <!-- Call to action -->
    <div class="text-center">
      <a 
        href="#registro" 
        class="inline-flex items-center justify-center rounded-full bg-red-800 px-6 py-3 text-base font-medium text-white shadow-md hover:bg-red-900 focus:outline-none focus:ring-2 focus:ring-red-800 focus:ring-offset-2 transition-all ease-in-out duration-200"
      >
        Comienza tu diagnóstico
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5 ml-2" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M10.293 5.293a1 1 0 011.414 0l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414-1.414L12.586 11H5a1 1 0 110-2h7.586l-2.293-2.293a1 1 0 010-1.414z" clip-rule="evenodd" />
        </svg>
      </a>
      <p class="mt-4 text-sm text-gray-500">Cupos limitados - Nueva cohorte inicia el 15 de agosto</p>
    </div>
  </div>
</section>

<style>
  @keyframes float-slow {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }
  @keyframes float-medium {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-6px); }
  }
  .animate-float-slow {
    animation: float-slow 8s ease-in-out infinite;
  }
  .animate-float-medium {
    animation: float-medium 6s ease-in-out infinite;
  }
</style>